def resumer(name: str, status: str,remarks:str):
    # resumed=graph.invoke(Command(resume={"status":status,"remarks":remarks}),config={"configurable":{"thread_id":name}})
    # return resumed
    print(name,status,remarks)
    return